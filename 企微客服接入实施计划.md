# 企微客服接入实施计划

> 创建日期：2026-06-05  
> 状态：代码已实现，待运营配置与联调  
> 关联设计：`docs/superpowers/specs/2026-06-05-wework-customer-service-design.md`

---

## 一、目标与原则

### 要做什么

| 用户端入口 | 改前 | 改后 |
|-----------|------|------|
| 首页悬浮「客服」 | WebSocket 在线客服 | 企微（二维码 → 绑定后 API） |
| 我的 → 客服 | 同上 | 同上 |
| 商品详情 → 客服 | 同上 + 发送商品卡片 | 企微 + 复制商品信息 |
| 订单详情 → 投诉 | 小程序投诉表单 | 企微 + 复制订单信息 |
| 我的 → 投诉工单 | 投诉列表页 | **移除入口** |

### 不做什么

- **不改**投诉相关后端 API、数据库表
- **不改** `pages-cs` 客服工作台
- **不改** 用户 ↔ 接单员 订单聊天（`pages/chat/room`）
- **不改** 打手端逻辑

### 当前阶段

企微已开通，**小程序尚未绑定企微客服** → 第一阶段用**二维码弹窗**；绑定完成后切换 `auto` 模式即可 API 直跳。

---

## 二、执行顺序总览

```
步骤 0  准备企微二维码（运营侧，可并行）
步骤 1  后端：站点配置项 seed
步骤 2  小程序：siteStore 读取配置
步骤 3  小程序：CsContactModal 组件
步骤 4  小程序：useWeworkCs composable
步骤 5  小程序：替换 4 个页面入口
步骤 6  小程序：移除投诉路由 + 我的页菜单
步骤 7  管理后台：上传二维码并保存配置
步骤 8  联调测试
步骤 9  （可选）企微绑定后切换 API 模式
```

每步完成后在 `[ ]` 打勾，再进入下一步。

---

## 步骤 0：准备企微二维码（运营）

**负责人**：运营 / 企业管理员  
**可与其他步骤并行**

- [ ] 登录企业微信管理后台（**待你操作**）
- [ ] 进入 **客户联系 → 联系我**，创建客服联系方式
- [ ] 下载/截图「联系我」二维码
- [ ] 将二维码图片上传到 OSS/CDN（与现有商品图同一套上传流程）
- [ ] 记录图片 URL，供步骤 7 填入后台

> 若暂时没有二维码，可先完成代码开发，步骤 7 再补配置。

---

## 步骤 1：后端站点配置项

**文件**：`delta-game/delta-system/src/main/java/com/delta/system/controller/SysConfigController.java`  
**方法**：`ensureBuiltinConfigs()` 末尾追加

- [x] 新增 5 个 `ensureConfig`，`configGroup` 均为 `"站点配置"`

```java
ensureConfig("cs.contact_mode", "客服联系方式", "qrcode", "text", "站点配置",
        "qrcode=仅二维码；wework=仅API；auto=优先API失败降级二维码");
ensureConfig("cs.corp_id", "企微企业ID", "", "text", "站点配置",
        "企业微信 corpId，小程序绑定企微客服后填写");
ensureConfig("cs.service_url", "企微客服链接", "", "text", "站点配置",
        "微信客服 extInfo.url，绑定后从企微后台获取");
ensureConfig("cs.qrcode_url", "客服二维码", "", "image", "站点配置",
        "企微「联系我」二维码图片 URL");
ensureConfig("cs.contact_tips", "客服提示语", "长按识别二维码，添加客服微信", "text", "站点配置",
        "二维码弹窗底部说明文字");
```

- [ ] 重启后端服务（**待你操作**）
- [ ] 验证：访问 `GET /system/config/site` 响应中包含 `cs_qrcode_url` 等字段（**待你操作**）

---

## 步骤 2：小程序 siteStore

**文件**：`delta-mp/store/site.js`

- [x] 新增 5 个 ref：

```javascript
const csContactMode = ref('qrcode')
const csCorpId = ref('')
const csServiceUrl = ref('')
const csQrcodeUrl = ref('')
const csContactTips = ref('长按识别二维码，添加客服微信')
```

- [x] 在 `fetchSiteConfig()` 内赋值：

```javascript
if (data.cs_contact_mode) csContactMode.value = data.cs_contact_mode
if (data.cs_corp_id) csCorpId.value = data.cs_corp_id
if (data.cs_service_url) csServiceUrl.value = data.cs_service_url
if (data.cs_qrcode_url) csQrcodeUrl.value = data.cs_qrcode_url
if (data.cs_contact_tips) csContactTips.value = data.cs_contact_tips
```

- [x] `return` 中导出上述字段

> 配置 key 使用下划线形式（`cs_contact_mode`），与现有 `site_name` 等保持一致。

---

## 步骤 3：CsContactModal 组件

**新建文件**：`delta-mp/components/CsContactModal.vue`

- [x] 底部滑出弹窗（参考 `CouponPicker.vue` 样式）
- [x] Props：`visible`、`title`、`qrcodeUrl`、`tips`、`copyText`（可选）
- [x] 展示二维码 `image`（`mode="widthFix"`）
- [x] 有 `copyText` 时显示「复制信息」按钮
- [x] 点击复制调用 `uni.setClipboardData`，成功 toast「已复制，添加客服后粘贴发送」
- [x] 点击遮罩 / 关闭按钮关闭弹窗
- [x] 支持 `v-model:visible`

**UI 结构参考**：

```
┌─────────────────────────────┐
│  联系客服 / 投诉反馈      ✕  │
├─────────────────────────────┤
│        [二维码图片]          │
│   长按识别二维码，添加客服    │
│      [ 复制订单信息 ]        │  ← 有 copyText 时显示
└─────────────────────────────┘
```

---

## 步骤 4：useWeworkCs composable

**新建文件**：`delta-mp/composables/useWeworkCs.js`

- [x] 导出 `useWeworkCs()`，返回 `{ modalState, openWeworkCs }`
- [x] `modalState`：`visible`、`title`、`qrcodeUrl`、`tips`、`copyText`
- [x] 实现 `buildCopyText(scene, order, product)`：

| scene | 文案模板 |
|-------|---------|
| `general` | 空字符串 |
| `product` | `【商品咨询】{name} ¥{price}（ID:{id}）` |
| `complaint` | `【投诉】订单号:{orderNo} 商品:{productName} 状态:{status}` |

- [x] 实现 `openWeworkCs({ scene, order, product })` 主流程：

```
1. 从 siteStore 读配置
2. qrcodeUrl 为空 → toast「客服配置中，请稍后再试」→ return
3. contactMode 为 wework 或 auto，且 corpId + serviceUrl 齐全：
   #ifdef MP-WEIXIN
   → wx.openCustomerServiceChat({ corpId, extInfo: { url: serviceUrl }, ... })
   → 成功则 return
   → 失败则继续降级
   #endif
4. 打开 CsContactModal（设置 title / copyText / qrcodeUrl / tips）
5. complaint / product 场景：有 copyText 时弹窗显示复制按钮
```

- [ ] `openCustomerServiceChat` 可选参数（绑定后生效）：
  - `showMessageCard: true`（有 order 或 product 时）
  - `sendMessageTitle`：商品名或订单号
  - `sendMessagePath`：对应小程序页面 path

- [ ] 弹窗标题映射：
  - `general` → `联系客服`
  - `product` → `咨询商品`
  - `complaint` → `投诉反馈`

---

## 步骤 5：替换页面入口

### 5.1 首页 `delta-mp/pages/index/index.vue`

- [x] 删除 `import { createCsSession } from '@/api/chat'`
- [x] 引入 `useWeworkCs`、`CsContactModal`
- [x] `goCustomerService` 改为：

```javascript
function goCustomerService() {
  if (!userStore.checkLogin()) return
  openWeworkCs({ scene: 'general' })
}
```

- [x] 模板底部挂载 `CsContactModal`

### 5.2 我的页 `delta-mp/pages/mine/index.vue`

- [x] 同上替换 `goCustomerService`
- [x] **删除**「我的投诉工单」菜单项
- [x] 挂载 `CsContactModal`

### 5.3 商品详情 `delta-mp/pages/product/detail.vue`

- [x] 删除 `createCsSession`、`sendChatMessage` 相关 import 与逻辑
- [x] `goChat` 改为：

```javascript
function goChat() {
  if (!userStore.checkLogin()) return
  openWeworkCs({ scene: 'product', product: product.value })
}
```

- [x] 挂载 `CsContactModal`

### 5.4 订单详情 `delta-mp/pages/order/detail.vue`

- [x] **保持** `goChat` 不变（跳转接单员聊天）
- [x] `goComplaint` 改为：

```javascript
function goComplaint() {
  openWeworkCs({ scene: 'complaint', order: order.value })
}
```

- [x] 挂载 `CsContactModal`

> **可选优化**：在 `App.vue` 全局挂载一份 `CsContactModal`，各页面只调 composable，避免重复组件。首版各页挂载亦可。

---

## 步骤 6：移除用户端投诉路由

**文件**：`delta-mp/pages.json`

- [x] 删除以下 3 条 `pages` 注册（保留 `.vue` 源文件即可）：
  - `pages/complaint/create`
  - `pages/complaint/detail`
  - `pages/complaint/list`

- [ ] 确认 `pages-cs` 分包内投诉页 **保留不动**：
  - `pages-cs/complaint/list`
  - `pages-cs/complaint/handle`

- [ ] 全局搜索是否还有跳转用户端投诉页的链接，一并清理：

```bash
# 预期仅剩 pages-cs 和已删除的源文件
rg "pages/complaint" delta-mp --glob "*.vue" --glob "*.js"
```

---

## 步骤 7：管理后台配置

**入口**：管理后台 → 系统配置 → **站点配置** 分组

- [ ] `客服联系方式` → `qrcode`
- [ ] `客服二维码` → 上传步骤 0 准备的图片
- [ ] `客服提示语` → 按需修改（默认即可）
- [ ] `企微企业ID`、`企微客服链接` → 暂留空
- [ ] 保存配置

- [ ] 小程序重新编译，确认 `fetchSiteConfig` 能拉到 `cs_qrcode_url`

---

## 步骤 8：联调测试清单

### 二维码模式（当前）

- [ ] 首页悬浮客服 → 弹出二维码
- [ ] 我的 → 客服 → 弹出二维码
- [ ] 商品详情 → 客服 → 弹出二维码 + 可复制商品信息
- [ ] 订单详情 → 投诉 → 弹出二维码 + 可复制订单信息
- [ ] 订单详情 → 聊天 → 仍进入与接单员的聊天页
- [ ] 我的页无「投诉工单」入口
- [ ] 未配置二维码时 toast「客服配置中，请稍后再试」
- [ ] `pages-cs` 投诉管理页仍可正常打开

### API 模式（绑定后，步骤 9）

- [ ] 改 `cs.contact_mode` 为 `auto`
- [ ] 填写 `cs.corp_id`、`cs.service_url`
- [ ] 点击客服直接打开微信客服会话
- [ ] API 失败时自动降级二维码弹窗

---

## 步骤 9：企微绑定升级（后续）

**前置**：小程序管理后台完成「微信客服 → 绑定企业微信」

- [ ] 企微后台获取 `corpId`、客服链接
- [ ] 管理后台填写 `cs.corp_id`、`cs.service_url`
- [ ] `cs.contact_mode` 改为 `auto`
- [ ] 真机验证 API 直跳 + 降级逻辑

---

## 附录 A：涉及文件清单

### 新建

| 文件 | 说明 |
|------|------|
| `delta-mp/composables/useWeworkCs.js` | 统一客服入口 |
| `delta-mp/components/CsContactModal.vue` | 二维码弹窗 |

### 修改

| 文件 | 说明 |
|------|------|
| `delta-game/.../SysConfigController.java` | 配置项 seed |
| `delta-mp/store/site.js` | 读取客服配置 |
| `delta-mp/pages/index/index.vue` | 首页客服 |
| `delta-mp/pages/mine/index.vue` | 我的客服 + 删投诉菜单 |
| `delta-mp/pages/product/detail.vue` | 商品客服 |
| `delta-mp/pages/order/detail.vue` | 订单投诉 |
| `delta-mp/pages.json` | 移除投诉路由 |

### 不动

| 路径 | 原因 |
|------|------|
| `delta-mp/pages/complaint/*.vue` | 源文件保留，仅注销路由 |
| `delta-mp/pages-cs/complaint/**` | 客服内部管理 |
| `delta-mp/pages/chat/**` | 订单聊天 |
| `delta-mp/api/complaint.js` | 后端接口保留 |
| 投诉相关 Java 代码 / 数据库 | 无真实数据，暂不改动 |

---

## 附录 B：配置项速查

| 后台字段 | configKey | 阶段一 | 阶段二 |
|---------|-----------|--------|--------|
| 客服联系方式 | `cs.contact_mode` | `qrcode` | `auto` |
| 企微企业ID | `cs.corp_id` | 空 | 填写 |
| 企微客服链接 | `cs.service_url` | 空 | 填写 |
| 客服二维码 | `cs.qrcode_url` | **必填** | 保留作降级 |
| 客服提示语 | `cs.contact_tips` | 默认文案 | 可选改 |

---

## 附录 C：进度记录

| 步骤 | 负责人 | 完成日期 | 备注 |
|------|--------|---------|------|
| 0 准备二维码 | | | |
| 1 后端配置项 | AI | 2026-06-05 | 代码已提交 |
| 2 siteStore | AI | 2026-06-05 | 代码已提交 |
| 3 CsContactModal | AI | 2026-06-05 | 代码已提交 |
| 4 useWeworkCs | AI | 2026-06-05 | 代码已提交 |
| 5 替换入口 | AI | 2026-06-05 | 4 个页面已改 |
| 6 移除投诉路由 | AI | 2026-06-05 | pages.json 已改 |
| 7 后台配置 | | | |
| 8 联调测试 | | | |
| 9 API 升级 | | | |

---

*按步骤 1 → 8 顺序开发即可上线二维码方案；步骤 0、9 由运营在绑定企微后独立完成。*
